nano ~/.zshrc

cd() {
    builtin cd "$@" || return
    files=("README" "Parkinfo" "Lakeinfo" "insidethehouse" "relax" "KitchenOptions" "Storeinfo")

    for f in "${files[@]}"; do
        if [ -f "$f" ]; then
            less "$f"
        fi
    done
}


source ~/.zshrc

